[project]
name = "stello-weather"
version = "0.1.0"
description = "A NiceGUI 7-day weather forecast by US ZIP code — a stello dogfood app."
requires-python = ">=3.11"
dependencies = [
    "nicegui>=1.4",
    "httpx>=0.24",
]
